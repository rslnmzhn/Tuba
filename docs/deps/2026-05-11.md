# Dependency audit — 2026-05-11

## Summary
package_managers: pub / Flutter (pubspec.yaml, pubspec.lock); no npm, yarn, pnpm, pip/uv, go modules, cargo, maven, or gradle manifest detected by the requested scan order
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 0
outdated_minor: 0
unlocked: 0

## Scan commands
- `flutter pub outdated --json`
- `dart pub outdated --json`
- `dart pub deps --json`
- `flutter --version && dart --version`

## Vulnerabilities

No known affected current packages were reported by Dart/Flutter pub metadata (`isCurrentAffectedByAdvisory: false` for all reported packages).

## Outdated (major version gaps only)
| package | current | latest | breaking_changes |
|---------|---------|--------|-----------------|

No direct dependency major-version gaps found.

## Outdated direct dependencies

No outdated direct hosted dependencies were reported. `flutter` and `flutter_test` are SDK dependencies and are governed by the installed Flutter SDK.

## Transitive outdated packages

The following outdated packages were reported as transitive only, mostly constrained by the Flutter SDK/test stack and not direct project dependencies:

| package | current | latest | notes |
|---------|---------|--------|-------|
| characters | 1.4.0 | 1.4.1 | transitive via Flutter SDK |
| matcher | 0.12.17 | 0.12.20 | transitive via flutter_test |
| material_color_utilities | 0.11.1 | 0.13.0 | transitive via Flutter SDK; latest crosses major version because 0.x minor changes may be breaking |
| meta | 1.17.0 | 1.18.2 | transitive via Flutter/test packages |
| test_api | 0.7.7 | 0.7.12 | transitive via flutter_test |
| vector_math | 2.2.0 | 2.3.0 | transitive via Flutter SDK |

## Unlocked versions
| package | spec | risk |
|---------|------|------|

No `*` or `latest` dependency specs found. Direct hosted dependencies use caret constraints and are resolved through `pubspec.lock`.

## Risky patterns

- Deprecated direct dependencies: none reported by pub metadata.
- Production dependency on dev-only tooling: none found; `flutter_lints` and `flutter_test` are in `dev_dependencies`.
- Direct packages with large dependency trees: none found among hosted direct dependencies; `cupertino_icons` has no transitive dependencies.
- Packages last published more than 2 years ago with open security issues: not identified from available pub metadata.

## Blockers / limitations

- The requested audit matrix does not include Dart/Flutter `pub`; this report used `dart pub` / `flutter pub` metadata instead.
- No dedicated Dart vulnerability audit command equivalent to `npm audit` was available in the requested tool list. Advisory status was taken from `dart pub outdated --json` / `flutter pub outdated --json` fields.
- No package upgrades or lockfile changes were performed.
