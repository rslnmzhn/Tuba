# Dependency audit — 2026-05-12

## Summary
package_managers: dart pub / Flutter (`pubspec.yaml`, `pubspec.lock`)
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 0
outdated_minor: 0
unlocked: 0

Notes:
- No package managers from the required manifest list were found (`npm`, `yarn`, `pnpm`, `pip/uv`, Go modules, Cargo, Maven, Gradle).
- This Flutter repository uses Dart pub, so `flutter pub outdated --json` and `dart pub deps --json` were run as the applicable dependency checks.
- Dart pub reported no packages with `isCurrentAffectedByAdvisory: true`, no discontinued packages, and no retracted current versions.

## Vulnerabilities

None found by the available Dart pub advisory metadata.

---

## Outdated (major version gaps only)
| package | current | latest | breaking_changes |
|---------|---------|--------|------------------|
| None | - | - | - |

## Unlocked versions
| package | spec | risk |
|---------|------|------|
| None | - | - |

## Additional staleness observations

The following transitive packages have newer releases but do not meet the audit threshold for major-version gaps or being more than two minor versions behind:

| package | kind | current | latest | advisory_flag |
|---------|------|---------|--------|---------------|
| characters | transitive | 1.4.0 | 1.4.1 | false |
| matcher | transitive | 0.12.17 | 0.12.20 | false |
| material_color_utilities | transitive | 0.11.1 | 0.13.0 | false |
| meta | transitive | 1.17.0 | 1.18.2 | false |
| test_api | transitive | 0.7.7 | 0.7.12 | false |
| vector_math | transitive | 2.2.0 | 2.3.0 | false |

## Risky patterns

- Direct deprecated/discontinued package: none reported by `flutter pub outdated --json`.
- Production dependency on a dev-only tool: none found. `flutter_lints` and `flutter_test` are in `dev_dependencies`.
- Large transitive dependency tree from direct production dependency: none identified. The only hosted direct production dependency, `cupertino_icons`, has no dependencies in `dart pub deps --json`.
- Duplicate logical packages at multiple versions: none found in `pubspec.lock`.

## Commands run

- `flutter pub outdated --json`
- `dart pub deps --json`
- Manifest discovery for npm/yarn/pnpm/pip/go/cargo/maven/gradle and Dart pub files
