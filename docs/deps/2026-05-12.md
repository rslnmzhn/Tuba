# Dependency audit — 2026-05-12

## Summary
package_managers: dart/pub (Flutter) detected; CMake FetchContent detected for native third-party source; no npm, yarn, pnpm, pip/uv, go modules, cargo, maven, or gradle manifests found
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 0
outdated_minor: 0
unlocked: 0

## Scope
- Found Flutter/Dart manifests: `pubspec.yaml`, `pubspec.lock`.
- The prescribed audit manifest set did not include Dart/pub, so the Dart dependency graph was checked with `dart pub outdated --json` and `dart pub deps --json` as a supplemental scan.
- No package-manager manifests were found for npm, yarn, pnpm, pip/uv, go modules, cargo, maven, or gradle.
- Native CMake currently uses `FetchContent` for `mbedtls` from `https://github.com/Mbed-TLS/mbedtls.git` at `GIT_TAG v3.6.2`.

## Vulnerabilities

None found in the available Dart/pub metadata. `dart pub outdated --json` reported `isCurrentAffectedByAdvisory: false` for all listed packages.

---

## Outdated (major version gaps only)
| package | current | latest | breaking_changes |
|---------|---------|--------|-----------------|

No major version gaps found.

## Outdated (supplemental non-major observations)
| package | kind | current | latest | notes |
|---------|------|---------|--------|-------|
| characters | transitive | 1.4.0 | 1.4.1 | newer version incompatible with current constraints |
| matcher | transitive | 0.12.17 | 0.12.20 | newer version incompatible with current constraints |
| material_color_utilities | transitive | 0.11.1 | 0.13.0 | 0.x minor gap; may carry breaking-change risk under pre-1.0 semver |
| meta | transitive | 1.17.0 | 1.18.2 | newer version incompatible with current constraints |
| test_api | transitive | 0.7.7 | 0.7.12 | newer version incompatible with current constraints |
| vector_math | transitive | 2.2.0 | 2.3.0 | newer version incompatible with current constraints |

## Unlocked versions
| package | spec | risk |
|---------|------|------|

No `*` or `latest` dependency specs found in `pubspec.yaml`.

## Risky patterns
- Deprecated direct dependencies: none reported by available Dart/pub metadata.
- Production dependency on dev-only tools: none found; `flutter_lints` and `flutter_test` are in `dev_dependencies`.
- Large direct dependency trees: none found among direct hosted dependencies; `cupertino_icons` has no transitive dependencies.
- Duplicate installed versions: none found in the Dart dependency graph.
- Native FetchContent pinning: `mbedtls` is pinned to a version tag (`v3.6.2`) but not to an immutable commit SHA or archive hash.

## Native third-party dependency caution
- Before adding `openh264` and `libyuv` via `FetchContent`, pin each dependency to an immutable commit SHA or verified archive hash rather than a branch or floating tag.
- Record upstream license obligations and redistribution constraints. `openh264` has codec/patent and binary redistribution considerations; `libyuv` is source-distribution friendly but still needs license notice tracking.
- Keep native dependency orchestration in `native/CMakeLists.txt` and re-run this audit after adding them, because no C/C++ vulnerability audit manifest currently covers `FetchContent` sources.
- Consider adding an explicit native dependency inventory under docs or build metadata so future audits can identify source URL, pinned revision, license, and update cadence.

## Blockers / limitations
- Standard vulnerability audit tools were skipped because no supported manifests were present for npm, yarn, pnpm, pip/uv, go modules, cargo, maven, or gradle.
- `dart pub audit --json` is not available in the installed Dart SDK; vulnerability status is limited to advisory flags exposed by `dart pub outdated --json`.
- No C/C++ third-party dependency manifest for openh264/libyuv integration exists yet, so native dependency risk must be re-audited after adding those manifests or vendored source metadata.
- Existing CMake `FetchContent` dependency (`mbedtls`) is outside the supported package-manager audit tools and was not vulnerability-scanned by an automated C/C++ advisory scanner.
