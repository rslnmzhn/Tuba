# Dependency audit — 2026-05-12

## Summary
package_managers: dart pub / Flutter (`pubspec.yaml`, `pubspec.lock`)
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 0
outdated_minor: 4
unlocked: 0

## Scope and tooling
- Standard package-manager manifests requested by the dependency-auditor instructions were checked: npm, yarn, pnpm, pip/uv, Go modules, Cargo, Maven, and Gradle manifests were not found.
- Flutter/Dart manifests were found and audited as the active dependency system for this repository.
- Vulnerability signal source: `dart pub outdated --show-all --json` advisory flags (`isCurrentAffectedByAdvisory`). No affected current packages were reported.
- Staleness source: `dart pub outdated --json` and `dart pub outdated --show-all --json`.
- Dependency graph source: `dart pub deps --json`.

## Vulnerabilities

None found by available Dart/pub advisory metadata.

---

## Outdated (major version gaps only)
| package | current | latest | breaking_changes |
|---------|---------|--------|------------------|
| None | - | - | - |

## Outdated minor version gaps
| package | kind | current | latest | notes |
|---------|------|---------|--------|-------|
| matcher | transitive | 0.12.17 | 0.12.20 | More than 2 patch releases behind; pulled via `flutter_test`/SDK test stack. |
| material_color_utilities | transitive | 0.11.1 | 0.13.0 | More than 2 minor versions behind; pulled via Flutter SDK. |
| meta | transitive | 1.17.0 | 1.18.2 | More than 2 patch releases behind; pulled transitively by SDK/test packages. |
| test_api | transitive | 0.7.7 | 0.7.12 | More than 2 patch releases behind; pulled via `flutter_test`. |

Additional non-threshold outdated transitive packages reported by pub: `characters` 1.4.0 -> 1.4.1, `vector_math` 2.2.0 -> 2.3.0.

## Unlocked versions
| package | spec | risk |
|---------|------|------|
| None | - | - |

## Duplicate versions

None found in the pub dependency graph.

## Risky patterns
- Direct deprecated/discontinued package dependencies: none reported by pub (`isDiscontinued: false` for all packages).
- Production dependency on dev-only tooling: none found; `flutter_lints` and `flutter_test` are in `dev_dependencies`.
- Direct packages with 50+ transitive dependencies: none found; direct hosted production package `cupertino_icons` has no transitive dependencies.
- Packages last published more than 2 years ago with open security issues: not confirmed; no open security advisories were reported by pub metadata.

## Blockers / limitations
- No npm, yarn, pnpm, pip/uv, Go, Cargo, Maven, or Gradle manifests were found at repository root, so their scanners were not run.
- The auditor instruction set does not include Dart/pub, but this Flutter project uses pub; the report includes a best-effort Dart/pub audit to cover the actual dependency manifests present.
- `dart pub outdated` reports advisory presence but not severity buckets; because no current package was advisory-affected, all vulnerability severity counts are zero.
