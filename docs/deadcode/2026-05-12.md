# Dead-code scan — 2026-05-12

## Summary
orphaned_files: 1
dead_exports: 4
unreachable_functions: 2
stale_flags: 0
tool_used: flutter analyze; cmake build; grep-only manual scan

## Findings

### native/src/nat_transport.h — medium
file: native/src/nat_transport.h
line: 10
type: orphaned_file
safe_to_delete: true
notes: NatTransport is a header-only stub with no includes or references outside its own file.

---

### rc::transport::UdpDiscovery::announce_once — medium
file: native/src/udp_discovery.cpp
line: 19
type: dead_export
safe_to_delete: true
notes: The static discovery announcement API is declared in udp_discovery.h but no project code includes that header or calls announce_once.

---

### rc::transport::UdpDiscovery::query_once — medium
file: native/src/udp_discovery.cpp
line: 46
type: dead_export
safe_to_delete: true
notes: The static discovery query API is declared in udp_discovery.h but no project code includes that header or calls query_once.

---

### rc::transport::shutdown_sockets — medium
file: native/src/socket_platform.cpp
line: 19
type: dead_export
safe_to_delete: true
notes: shutdown_sockets is declared in socket_platform.h and implemented in socket_platform.cpp, but grep found no call sites.

---

### rc::transport::socket_would_block — medium
file: native/src/socket_platform.cpp
line: 62
type: dead_export
safe_to_delete: true
notes: socket_would_block is declared in socket_platform.h and implemented in socket_platform.cpp, but grep found no call sites.

---

### rc::capture::X11Capture — low
file: native/src/x11_capture.h
line: 8
type: unreachable_function
safe_to_delete: maybe
notes: X11Capture and its methods are compiled for Linux but the platform capture factory currently instantiates only PipeWireCapture and no code references X11Capture.

---

### rc::capture::FrameBuffer::empty — low
file: native/src/frame_buffer.h
line: 22
type: unreachable_function
safe_to_delete: true
notes: FrameBuffer::empty is an inline helper, but grep found no project call sites.

---
