# Dead-code scan — 2026-05-12

## Summary
orphaned_files: 0
dead_exports: 2
unreachable_functions: 4
stale_flags: 0
tool_used: flutter analyze + grep

## Findings

### rc::transport::UdpDiscovery — medium
file: native/src/udp_discovery.h
line: 8
type: dead_export
safe_to_delete: true
notes: The class is declared in a private native header, but grep found no project references outside its own declaration and implementation.

---

### rc::transport::NatTransport — medium
file: native/src/nat_transport.h
line: 10
type: dead_export
safe_to_delete: false
notes: The class is only pulled in by an include in rc_api.cpp and is never instantiated, but AGENTS.md documents it as a transport scaffold stub behind C ABI exports.

---

### rc::transport::UdpDiscovery::announce_once — low
file: native/src/udp_discovery.cpp
line: 19
type: unreachable_function
safe_to_delete: true
notes: The static method is defined and declared, but grep found no call sites in the repository.

---

### rc::transport::UdpDiscovery::query_once — low
file: native/src/udp_discovery.cpp
line: 46
type: unreachable_function
safe_to_delete: true
notes: The static method is defined and declared, but grep found no call sites in the repository.

---

### rc::transport::shutdown_sockets — low
file: native/src/socket_platform.cpp
line: 19
type: unreachable_function
safe_to_delete: maybe
notes: The platform helper is declared and defined, but grep found no project call sites; it may be kept for paired socket lifecycle cleanup.

---

### rc::transport::socket_would_block — low
file: native/src/socket_platform.cpp
line: 62
type: unreachable_function
safe_to_delete: maybe
notes: The platform helper is declared and defined, but grep found no project call sites and no obvious dynamic references.

---
